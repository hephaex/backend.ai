apiVersion: v1
kind: Secret
metadata:
  name: {{ include "backend-ai-storage-proxy.fullname" . }}
  labels:
    {{- include "backend-ai-storage-proxy.labels" . | nindent 4 }}
type: Opaque
data:
  manager-password: {{ .Values.config.manager.password | b64enc }}
  {{- if .Values.config.database.password }}
  database-password: {{ .Values.config.database.password | b64enc }}
  {{- end }}
  {{- if .Values.config.redis.password }}
  redis-password: {{ .Values.config.redis.password | b64enc }}
  {{- end }}
  {{- if .Values.config.etcd.password }}
  etcd-password: {{ .Values.config.etcd.password | b64enc }}
  {{- end }}